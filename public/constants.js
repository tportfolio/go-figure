// TODO: is there a way to consolidate the channel constants across public and src?
const RECEIVER_CHANNEL = "fromMain";
const SENDER_CHANNEL = "toMain";

module.exports = {
    RECEIVER_CHANNEL,
    SENDER_CHANNEL
};