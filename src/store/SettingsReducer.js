const initialState = {
};

export const handleSettings = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default handleSettings;